// Crea un array vuoto.
//  Chiedi per 6 volte all'utente di inserire un numero,
//  se è dispari inseriscilo nell'array.

const empytArray = [' '];
let insertEl = prompt ('type a number');

if ( 2 % === 1){
    empytArray.push(insertEl);
    console.log(empytArray);
}